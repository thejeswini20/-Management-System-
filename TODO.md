# TODO

## Students UI Extraordinary Upgrade

- [ ] Refactor `frontend/src/pages/dashboard/Students.js` to fully reuse existing global design system (`index.css`, `Dashboard.css`).
- [ ] Upgrade Stats section to use `.stat-grid` + `.stat-card`.
- [ ] Upgrade course distribution card styling and bar visuals.
- [ ] Improve table/card views to match existing premium styles.
- [ ] Add loading + empty states (no results, loading data).
- [ ] Upgrade modal UI to match shared `.modal-overlay` / `.modal` styling and use floating-label form fields.
- [ ] Remove redundant inline styles where possible.
- [x] Run frontend to visually verify (table/card toggle, search/filter, CRUD modal, delete confirmation).
